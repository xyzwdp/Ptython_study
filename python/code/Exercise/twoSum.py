# _*_ coding:UTF-8 _*_
class Sum:
    def twosum(self,num1,num2):
        """

        :param num1: list[int]
        :param num2: list[int]
        :return: list[int]
        """
        #设置一个空列表
        result=[]
        # 判断循环取哪个
        if len(num1)>len(num2):
            for i in range(len(num1)):
                sum=num1.index[i]+num2.index[i]
        else:
            pass
